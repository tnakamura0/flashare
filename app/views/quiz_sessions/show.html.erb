<% content_for(:title, t(".title")) %>
<div class="mt-24 mb-8 px-6 md:px-10 lg:px-16 py-4 md:py-8 rounded-2xl md:rounded-3xl bg-violet-50">
  <h1 class="font-bold text-2xl md:text-3xl"><%= t(".title") %></h1>
  <p class="font-bold mt-2"><%= t(".result", total_questions: @quiz_session.total_questions, correct_number: @correct_number) %></p>
  <div class="mt-4">
    <%= link_to t(".to_index_question_page"), quiz_sessions_path %>
  </div>
</div>
